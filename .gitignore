# VM credentials and SSH keys
vm-credentials.conf
.vm_key
.vm_key.pub

# VM disk images (large binaries, regenerate per CLAUDE.md)
*.qcow2
*.img
*.img.xz

# QEMU runtime files
vm-console.log
vm.pid
nohup.out

# QEMU build artifacts
qemu/build/

# Local build artifacts
lib/compiledd/compiledd
lib/libtmatmulrt/*.o
lib/libtmatmulrt/*.so
test/rvt2_test
test/rvt2_bench
test/rvt2_cxl_test

# Humanize local loop state
.humanize/rlcr/
